import React from 'react';
import styled from 'styled-components';

const Strength = () => {
  return <Container>Strength</Container>;
};

export default Strength;

const Container = styled.div`
  display: flex;
  flex-direction: column;
`;
